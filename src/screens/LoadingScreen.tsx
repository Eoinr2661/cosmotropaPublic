import React from 'react';
import { View, Text } from 'react-native';

/**
 * This is the loading screen, originally it had its use for the more intense parts of the application, but isn't used presently
 * Keeping here for future implementations
 */
const LoadingScreen = () => {
  return (
    <View>
      <Text>
      Loading Screen
      </Text>
    </View>
  );
};

export default LoadingScreen;